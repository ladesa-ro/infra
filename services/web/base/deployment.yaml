apiVersion: apps/v1
kind: Deployment
metadata:
  name: ladesaro-web
spec:
  replicas: 1
  selector:
    matchLabels:
      name: ladesaro-web
  template:
    metadata:
      labels:
        name: ladesaro-web
    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxSurge: 1
        maxUnavailable: 0
    spec:
      containers:
        - name: ladesaro-web
          image: "registry.ladesa.com.br/web"
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: ladesaro-web-configmap
          resources:
            requests:
              cpu: "50m"
              memory: "1Gi"
            limits:
              memory: "2Gi"
              cpu: "2"
