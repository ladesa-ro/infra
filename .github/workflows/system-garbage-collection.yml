name: "Maintenance - Garbage Collection"

on:
  schedule:
    - cron: "10-40/23 * * * *"
  workflow_dispatch:

jobs:
  system-cleanup:
    runs-on:
      group: maintenance
    steps:
      - uses: actions/checkout@v4
      - run: ./utils/collect-garbage.sh
